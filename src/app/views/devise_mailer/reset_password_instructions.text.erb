<%= Settings.brand.name %>のパスワードのリセットを受け付けました。

こちらのページから新しいパスワードの登録を行ってください。
<%= edit_password_url(
    @resource,
    reset_password_token: @token,
    config: message['client-config'].to_s,
    redirect_url: message['redirect-url'].to_s
  ).html_safe %>

<%= render 'mailer/brand_signature' %>
