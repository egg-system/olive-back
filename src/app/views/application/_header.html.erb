<nav class="navbar navbar-dark bg-primary navbar-expand">
  <%= link_to 'olive system', home_path, :class => 'navbar-brand' %>
  <div class="navbar-collapse">
    <% if staff_signed_in? %>  
      <ul class="navbar-nav">
        <li class="nav-item dropdown">
          <a 
            id="shift-menu"
            class="nav-link dropdown-toggle"  
            data-toggle="dropdown" 
          >
            シフト管理
          </a>
          <div class="dropdown-menu" aria-labelledby="shift-menu">
            <%= link_to 'インポート', new_shift_path, :class => "dropdown-item" %>
            <%= link_to '一覧', shifts_path, :class => "dropdown-item" %>
          </div>
        </li>
        <li class="nav-item">
          <%= 
            link_to '予約管理', 
              reservations_path, 
              :class => "nav-link" 
          %>
        </li>
        <li class="nav-item">
          <%= 
            link_to 'スタッフ管理', 
              staffs_path, 
              :class => "nav-link" 
          %>
        </li>
        <li class="nav-item">
          <%= 
            link_to '顧客管理', 
              customers_path, 
              :class => "nav-link" 
          %>
        </li>
        <li class="nav-item">
          <%= 
            link_to 'ログアウト', 
              destroy_staff_session_path, 
              method: :delete,
              :class => "nav-link" 
          %>
        </li>
      </ul>
    <% end %>
  </div>
</nav>

<% if notice %>
  <p class="alert alert-info text-center" role="alert">
    <%= notice %>
  </p>
<% end %>
<% if alert %>
  <p class="alert alert-danger text-center" role="alert">
    <%= alert %>
  </p>
<% end %>